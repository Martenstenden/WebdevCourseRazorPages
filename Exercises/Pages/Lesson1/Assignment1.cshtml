@page
@model Exercises.Pages.Lesson1.Assignment1

@{
    Layout = null;
}

<div>
    <p>
        <span id="scoreHome">@Model.scoreHome</span> - <span id="scoreAway">@Model.scoreAway</span>
    </p>
    <div>
        <a id="incrementHome" href="?incrementHome=@Model.scoreHome">Home ++</a>
        <a id="decrementHome" href="?decrementHome=@Model.scoreHome">Home --</a>

        <a id="incrementAway" href="?incrementAway=@Model.scoreAway">Away ++</a>
        <a id="decrementAway" href="?decrementAway=@Model.scoreAway">Away --</a>
    </div>
    <div>
        <a id="reset" href="?reset=@Model.scoreAway @Model.scoreHome">Reset</a>
    </div>
</div>


